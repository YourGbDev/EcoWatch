<?php
/**
 * Application configuration.
 *
 * BASE_URL is the path (from the web server's document root) where the
 * EcoWatch application is installed. All internal redirects and links should
 * build URLs using this constant so the application works correctly even
 * when served from a sub-directory such as:
 *   http://localhost/smart-flood-waste-monitoring/
 *
 * Adjust this value if you move the project to a different folder.
 */
define('BASE_URL', '/smart-flood-waste-monitoring');
?>
